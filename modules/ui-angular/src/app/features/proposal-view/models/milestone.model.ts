export interface Milestone {
  clone: boolean;
  clonedMilestone: boolean;
  contributionChanged: boolean;
  createdDate: string;
  legDocumentName: string;
  proposalRef: string;
  status: string;
  title: string;
  updatedDate: number;
}
